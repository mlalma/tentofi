{
  "name": "tento-indexfactory",
  "version": "1.0.0",
  "description": "Create indices used to track values of underlyings in e.g. OTC derivatives",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mla_lma",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.12.5"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.5.1",
    "@openzeppelin/contracts": "^4.8.0",
    "dotenv": "^16.0.3"
  }
}
